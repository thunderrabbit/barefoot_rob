# Broken internal link targets grandfathered in by check_links.pl.
# Regenerate with: ./check_links.pl --update-baseline
# Run that ON THE MACHINE THAT RUNS deploy.sh AND THE PRE-PUSH HOOK:
# Hugo versions differ in where alias redirect stubs render, so a
# baseline from another machine reports phantom breaks here.
# (A machine with the journal submodule gives the most complete list.)
# Fix a link for real (./check_links.pl --find <url> shows where),
# then delete its line here.
